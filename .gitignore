# Python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# Env
.env
*.local

# Editor / OS
.DS_Store

# Data directory: ignore the uncompressed records (data/fhir_processed/ etc.), but
# DO track data/fhir_processed.tar.gz so a clone already has the folder + archive,
# ready to extract in place with no manual mkdir.
data/*
!data/fhir_processed.tar.gz

# Real wearable exports: backend/data/garmin_samples.json is a large real Garmin
# export (see app/wearable_source.py) — keep it out of git; the committed
# synthetic fallback in app/sample_data/ is used when it's absent.
backend/data/

# Conda environments
.conda/
env/
envs/
venv/
.venv/

# Environment variables and secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.local.php
.env.*
!.env.example


# models
.models/
models/