# Dashboard screenshots

These images are referenced from the root `README.md`. Capture at a 1440-wide
viewport in a real browser (the 3D map is WebGL and needs Google tiles, so it
won't render in a headless capture).

| File | What to capture |
| --- | --- |
| `overview.png` | The full live care map with the glass command bar and roster open |
| `checkin-data.png` | A patient detail panel on the **Check-in data** tab (conversation history) |
| `device-data.png` | A patient detail panel on the **Device data** tab (live vitals + wearable trends) |
| `patient-data.png` | A FHIR-backed patient (FHIR badge) on the **Patient data** tab (medical record) |

Re-exporting with the same filenames updates the README automatically.
