# Copy to .env to point the dashboard at a non-default backend.
VITE_API_URL=http://localhost:8000

# Google Maps / photorealistic 3D tiles API key for the Hong Kong city twin.
# Leave empty to fall back to the stylised (non-satellite) map.
VITE_GOOGLE_MAPS_API_KEY=
